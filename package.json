{
  "name": "aws-elb-cognito",
  "version": "1.0.0",
  "description": "mock server for aws cognito and elb",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.17.1"
  },
  "dependencies": {
    "aws-sdk": "^2.748.0",
    "nconf": "^0.10.0"
  }
}
